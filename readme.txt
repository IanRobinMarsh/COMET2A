Ideas.

1. Put it through our suite of tests
2. Build up a metric of guesses functional style "lapply(thisSeq, Osctests)"
3. Output 
4. Run and compare Bool and quantity (in and out of envelope)
5. Adjust heuristics / values
6. functional description


data -> in Matlab
read ->	data.table
butter


res(process(data + filter(butter(read(data)))))
plot(butter(read(data)))
