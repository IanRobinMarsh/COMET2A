signs <- function(vec, start, end) {

  print(paste("length", length(vec)))

  signs <- 0
  for (i in start:end) {
    if (vec[i] < 0)
	{signs[i] <- -1}
    else if (vec[i] >= 0)
         {signs[i] <- 1}
    else 
         {signs[i] <- 0}
  }
  return(signs)
}
